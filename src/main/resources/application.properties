spring.application.name=ApplicationMessagerie

# Run the backend on 8000 to match the frontend defaults and the browser error you're seeing.
server.port=8000

# JWT configuration used for signing tokens
jwt.secret=1cx3NyULn6FPy3ir6JDZ5o392y3LSm1H

# token validity in milliseconds (e.g., 86400000 = 1 day)
jwt.expiration-ms=86400000

# If you use docker-compose, adjust host/port accordingly.
spring.data.redis.url=redis://localhost:6379

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/messagingdb
spring.datasource.username=MessagingDB
spring.datasource.password=ziyad2004L

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true